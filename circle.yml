machine:
  java:
    version: oraclejdk8

dependencies:
  override:
    - mvn dependency:resolve

test:
  override:
    - mvn test -Dspring.datasource.url=jdbc:postgresql://127.0.0.1:5432/circle_test -Dspring.datasource.username=ubuntu -Dspring.datasource.password=password