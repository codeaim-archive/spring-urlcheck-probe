machine:
  java:
    version: oraclejdk8
  environment:
    DATABASE_URL: postgres://ubuntu:@127.0.0.1:5432/circle_test

dependencies:
  override:
    - mvn dependency:resolve

test:
  override:
    - mvn test