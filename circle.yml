machine:
  java:
    version: oraclejdk8

dependencies:
  override:
    - mvn dependency:resolve

test:
  override:
    - mvn test -B -Dspring.datasource.url=jdbc:postgres://ubuntu:@127.0.0.1:5432/circle_test -Dspring.datasource.driverClassName=org.postgresql.Driver